<div class="col-12 col-md-6 col-xl-4" id="widget-pay_item_totals">
    <div class="card h-100">
        <div class="card-header border-0 pt-5 d-flex align-items-start justify-content-between flex-nowrap gap-3">
            <div class="card-title align-items-start flex-column m-0 min-w-0">
                <span class="card-label fw-bold text-gray-900 fs-4">{{ w.title }}</span>
                <span class="text-gray-500 fs-6 fw-normal mt-2">{{ w.description }}</span>
            </div>
            <div class="card-toolbar flex-shrink-0">
                <div class="d-flex justify-content-end align-items-center gap-2">
                    {% for link in w.links %}
                        {% if link.canView %}
                            <a href="{{ path(link.route) }}" class="btn btn-sm btn-light" title="Open data tracking module">
                                {{ link.label }}
                            </a>
                        {% endif %}
                    {% endfor %}
                </div>
            </div>
        </div>
        <div class="card-body pt-0">
            <div class="scroll-y h-250px my-2 px-3">
                <div class="table-responsive">
                    <table class="table table-row-dashed table-row-gray-200 align-middle gs-0 gy-2">
                        <thead>
                        <tr class="fs-7 fw-bold text-gray-500 text-nowrap">
                            <th>Item</th>
                            <th>Quantity</th>
                            <th>Amount</th>
                        </tr>
                        </thead>
                        <tbody id="home-pay-item-totals-tbody">
                        {% for row in home_pay_item_totals %}
                            {% set amount = row.amount|default(0) %}
                            <tr>
                                <td class="text-gray-800">{{ row.name|default('—') }}</td>
                                <td class="text-gray-800">{{ row.quantity|default(0)|number_format(2, '.', ',') }}</td>
                                <td class="text-gray-800"><span class="home-money {{ amount > 0 ? 'home-money-positive' : (amount < 0 ? 'home-money-negative' : 'home-money-zero') }}">{{ amount < 0 ? '-$' : '$' }}{{ amount|abs|number_format(2, '.', ',') }}</span></td>
                            </tr>
                        {% else %}
                            <tr id="home-pay-item-totals-empty-row">
                                <td colspan="3" class="text-center text-muted py-5">No pay item totals in this range.</td>
                            </tr>
                        {% endfor %}
                        </tbody>
                    </table>
                </div>
            </div>
        </div>
    </div>
</div>
