<!DOCTYPE html PUBLIC "-//W3C//DTD XHTML 1.0 Transitional//EN" "http://www.w3.org/TR/xhtml1/DTD/xhtml1-transitional.dtd">
<html xmlns="http://www.w3.org/1999/xhtml">
   <head>
      <meta http-equiv="Content-Type" content="text/html; charset=utf-8" />
      <title>{{ asunto }}</title>

      <style>
         @import url('https://fonts.googleapis.com/css?family=Open+Sans:300,400,600,700,800');

         body {
            font-family: 'Open Sans', Helvetica, Arial, sans-serif;
            color: #000;
            margin: 0;
            font-weight: 300;
            font-size: 15px;
         }

         .container {
            width: 600px;
            margin: auto;
         }

         .row {
            margin: 0;
         }

         .centrado {
            margin: auto;
            float: none;
         }

         .text-center {
            text-align: center;
         }

         .header img {
            width: 180px;
         }

         a {
            color: #2c3691;
         }

         a:hover {
            text-decoration: none;
            color: #111;
         }

         p a {
            color: #2c3691;
            font-weight: bold;
         }

         .cabecera {
            color: #fff;
            padding: 8px;
            border-bottom: 1px solid #2c3691;
         }

         h1 {
            color: #000;
            text-transform: uppercase;
            font-size: 18px;
            margin-top: 15px;
            line-height: 2rem;
            text-align: center;
            font-weight: 700;
         }

         h3 {
            font-size: 15px;
            margin: 10px 0 5px 0;
            text-align: left !important;
         }

         p {
            text-align: left !important;
         }

         table {
            border: 1px solid #2c3691;
            border-radius: 4px;
            padding: 4px;
            font-size: 13px;
            line-height: 18px;
            font-weight: 300;
            border-spacing: 0;
            margin: 15px auto;
            width: 85%;
         }

         td,
         th {
            padding: 6px 10px;
            text-align: left !important;
         }

         th {
            background: #f5f5f5;
            font-weight: 600;
         }

         .footer {
            border-top: 1px solid #2c3691;
            margin-top: 1em;
            padding-top: 1em;
         }

         .footer p {
            text-align: center !important;
            font-size: 12px;
         }
      </style>
   </head>

   <body>
      <!-- cabecera -->
      <div class="header container centrado">
         <div class="row">
            <div class="text-center cabecera">
               <img src="{{ direccion_url }}/bundles/metronic8/img/logo.jpg" style="width: 180px" />
            </div>
         </div>
      </div>

      <!-- contenido -->
      <div class="titulos">
         <div class="container">
            <div class="row">
               <div class="centrado text-center">
                  <h3>Hello {{ usuario_nombre }},</h3>

                  <p>
                     We detected that the invoice for the project below is due on
                     <strong>{{ due_date }}</strong
                     >. There are <strong>{{ dias_restantes }}</strong> days remaining until the due date.
                  </p>

                  <p>
                     {{ mensaje_resumen }}
                  </p>

                  <table>
                     <tr>
                        <th>Project</th>
                        <td>
                           {% if project_number is not empty %}
                           {{ project_number }}
                           {% else %} Project #{{ project_id }}
                           {% endif %}
                        </td>
                     </tr>
                     <tr>
                        <th>Name</th>
                        <td>{{ project_name is not empty ? project_name : 'No name' }}</td>
                     </tr>
                     <tr>
                        <th>Due date</th>
                        <td>{{ due_date }}</td>
                     </tr>
                  </table>

                  <p>
                     Please sign in to the platform to review the project status and take any necessary actions:
                     <br />
                     <a href="{{ direccion_url }}" target="_blank" rel="noreferrer noopener">
                        {{ direccion_url }}
                     </a>
                  </p>

                  <br /><br />
               </div>
            </div>
         </div>
      </div>

      <!-- Footer -->
      <div class="footer container centrado">
         <div class="row">
            <div class="text-center">
               <p>* This message was generated automatically. Please do not reply.</p>
               <p>{{ "now"|date("Y") }} © data tracking</p>
            </div>
         </div>
      </div>
   </body>
</html>
